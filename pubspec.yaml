name: event_discovery_app
description: Event Discovery App
version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^3.4.1
  google_fonts: ^8.0.2
  intl: ^0.20.1
  shimmer: ^3.0.0

  firebase_core: ^3.13.1
  firebase_auth: ^5.5.1
  google_sign_in: ^6.2.2
  cloud_firestore: ^5.6.6
  file_picker: ^10.3.10
  flutter_map: ^8.2.2
  latlong2: ^0.9.1
  flutter_webrtc: ^1.4.1
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
